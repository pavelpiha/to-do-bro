/**
 * Main View Component - Handles the main menu view
 */
export class MainViewComponent {
  constructor(viewManager) {
    this.viewManager = viewManager;
  }

  // This component doesn't need much logic as it's primarily managed by TodoApp
  // The main logic is in the TodoApp setupMainMenuListeners method
}
